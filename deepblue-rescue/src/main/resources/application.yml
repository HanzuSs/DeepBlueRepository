spring:
  application:
    name: deepblue-rescue
  datasource:
    url: jdbc:postgresql://localhost:5432/deepblue_rescue
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
